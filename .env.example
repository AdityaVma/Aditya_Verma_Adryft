# Use http://localhost:3000 to proxy all /api/* requests to backend services (see next.config.js rewrites).
# Use http://localhost:3001 to talk to auth service only.
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_WS_URL=ws://localhost:3009
